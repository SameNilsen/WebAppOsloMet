@model IEnumerable<Comment>

@* List of all comments commented by a user *@

@{
    var user = ViewData["User"] as User;
    ViewData["Title"] = "User comments";
}

@{
    if (user == null)
    {
        <h1 style="text-align:center">Votes by user</h1>
    }
    else
    {
        <h1 style="text-align:center">Votes by @user.Name</h1>

    }
}

<table class="table table-striped">
    <tr>
        <th>Comment</th>
        <th>Date</th>
        <th>Post</th>
    </tr>

    <!-- Loop through all their comments -->
    @foreach (var comment in Model)
    {
        <tr>
            <td>@comment.CommentText</td>
            <td>@comment.PostDate</td>
            <td>
                <a asp-controller="Post"
                   asp-action="DetailedPost"
                   asp-route-id="@comment.PostID">@comment.Post.Title</a>
            </td>
        </tr>
    }
</table>
